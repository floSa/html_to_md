services:
  webapp:
    build: .
    image: fast_to_md
    ports:
      - "8505:8501"
    volumes:
      - ./FAST2MD:/app/FAST2MD
    environment:
      FAST2MD_ROOT: /app/FAST2MD
    restart: unless-stopped

  watcher:
    build: .
    image: fast_to_md
    command: ["python", "app/watcher.py"]
    volumes:
      - ./FAST2MD:/app/FAST2MD
    environment:
      FAST2MD_ROOT: /app/FAST2MD
      # Intervalle de scan en secondes (3600 = toutes les heures).
      WATCH_INTERVAL_SECONDS: "3600"
    restart: unless-stopped
    depends_on:
      - webapp
